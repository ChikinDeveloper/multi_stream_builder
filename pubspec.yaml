name: multi_stream_builder
description: Flutter widget to replace multiple nester StreamBuilder/ValueListenableBuilder, allowing to combine both
version: 0.0.1
homepage: https://chikin.run

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.2
  collection: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter: